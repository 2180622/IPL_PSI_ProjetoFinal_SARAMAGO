<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.saramago">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".vistas.leitores.EditLeitorActivity"></activity>
        <!-- Leitores -->
        <activity android:name=".vistas.leitores.AddLeitorActivity" />
        <activity android:name=".vistas.leitores.DetalhesLeitorActivity" />
        <activity android:name=".vistas.leitores.ListaLeitoresActivity" /> <!-- DASHBOARD -->
        <activity
            android:name=".vistas.MenuMainActivity"
            android:label=""
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" /> <!-- LOGIN -->
        <activity
            android:name=".vistas.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>